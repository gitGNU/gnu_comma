-- Ensure conflicting type declarations are not combined thru inheritance.
--
-- FIXME:  The compiler gives a very bad diagnostic here.

signature S with
   type E is (A);
end S;

signature T with
   type E is (B);               -- EXPECTED-ERROR: conflicts with declaration
end T;

signature U is S and T end U;

