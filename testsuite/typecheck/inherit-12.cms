-- Ensure redundant declarations match wrt parameter modes.

signature S with
   procedure P (X : in out %);
   procedure Q (X : in out %);
end S;

signature T is S
with
   procedure P (X : %);         -- EXPECTED-ERROR: conflicts with declaration
   procedure Q (Y : %);         -- EXPECTED-ERROR: conflicts with declaration
end T;
