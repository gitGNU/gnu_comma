-- MUST-FAIL: Ensure parameters of mode "in" cannot be used in a "in out"
-- context.

domain D add
   procedure P (X : in out Boolean) is
   begin
      X := true;
   end P;

   procedure Test (X : in Boolean) is
   begin
      P(X);
   end Test;
end D;

