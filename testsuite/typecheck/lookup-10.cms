-- MUST-FAIL:  Ensure identical exports due to instantiation are treated as
-- ambiguous.

signature S end S;

domain D with S; end D;

domain F (X : S) with
   procedure Foo (x : X);
   procedure Foo (x : D);
add
   procedure Foo (x : X) is begin end Foo;
   procedure Foo (x : D) is begin end Foo;
end F;

domain Test add
   procedure Test (x : D) is
   begin
      F(D)::Foo(x);
   end Test;
end Test;
