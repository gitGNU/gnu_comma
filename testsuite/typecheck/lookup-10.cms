-- Ensure the "exports" of a type are propagated by an import.

domain D with
   type T is range 1..10;
end D;

domain Test with
   procedure Run;
add
   procedure Run is
      import D;
      A : T := 1;
      B : T := A + 1;
   begin end Run;
end Test;
