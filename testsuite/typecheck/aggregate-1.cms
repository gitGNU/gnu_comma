-- MUST-FAIL: Ensure keyed aggregates lacking an others clause are continuous.

domain Test with
   procedure Run;
add
   procedure Run is
      S : String := (1..3 => 'x', 5..6 => 'y');
   begin end Run;
end Test;
