-- MUST-FAIL:  Ensure local declarations in an add cannot conflict with the
-- domains name or parameters.

domain D add
   type D is (A);

   type E is (D);

   function D return Integer is
   begin
      return 0;
   end D;

   procedure D is begin end D;
end D;
