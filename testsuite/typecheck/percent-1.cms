-- MUST-FAIL:  Ensure self-recursive instances are reported.

signature S with
   procedure Foo;
end S;

generic
   abstract domain X is S;
domain F is S add
   procedure Foo is
   begin
      F(%).Foo;
   end Foo;
end F;
