-- Ensure self-recursive instances are reported.

signature S with
   procedure Foo;
end S;

generic
   abstract domain X is S;
domain F is S add
   procedure Foo is
   begin
      F(%).Foo;                 -- EXPECTED-ERROR: recursive instantiation
   end Foo;
end F;
