-- Ensure super-signatures propagate enumeration literals via imports.

signature S with
   type T1 is (E1);
end S;

domain D with
  S;
end D;

domain E add
   procedure P is
      import D;
      V : T1 := E1;
   begin end P;
end E;
