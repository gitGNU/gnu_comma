-- Ensure super-signatures propagate enumeration literals via use clause.

signature S with
   type T1 is (E1);
end S;

domain D is S end D;

domain E add
   procedure P is
      use D;
      V : T1 := E1;
   begin end P;
end E;
